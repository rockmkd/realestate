CREATE TABLE apt(
    id INT AUTOINCREMENT,
    bast_dt TEXT,
    apt_id INT,
    apt_namt TEXT,
    dong TEXT,
    `floor` TEXT,
    price INT,
    desc TEXT,
    PRIMARY KEY(id)
)
base_dt, apt_id, apt_name, size, dong, floor, price, desc
